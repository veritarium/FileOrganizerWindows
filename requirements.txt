# File Organizer for Windows - Requirements
# Minimal dependencies for portable GUI application

# Build dependencies
pyinstaller>=5.13          # Create standalone executable

# Optional: Configuration (if needed for future features)
PyYAML>=6.0                # Configuration files

# Note: tkinter and sqlite3 are built into Python, no installation needed
# Note: hashlib is part of Python standard library
